// Arquivo de códgos comentados para consulta de possíveis implementações.


//Number Picker
/*const Text("Número de Mecânicos"),
                NumberPicker(
                  haptics: true,
                  selectedTextStyle: const TextStyle(
                      color: blue, fontSize: 32, fontWeight: FontWeight.bold),
                  axis: Axis.horizontal,
                  minValue: 1,
                  maxValue: 4,
                  value: _currentValue,
                  onChanged: (value) => setState(() => _currentValue = value),
                ),
                CustomTextFormField(),*/
                /* DropdownButton(
                  dropdownColor: lightyellow,
                  
                  value: dropdownValue,
                  icon: const Icon(Icons.arrow_downward),
                  elevation: 16,
                  style: const TextStyle(color: Colors.deepPurple),
                  underline: Container(
                    height: 2,
                    color: Colors.deepPurpleAccent,
                  ),
                  onChanged: (value) =>
                      setState(() => dropdownValue = value.toString()),
                  items: <String>['One', 'Two', 'Free', 'Four']
                      .map<DropdownMenuItem<String>>((String value) {
                    return DropdownMenuItem<String>(
                      value: value,
                      child: Text(value),
                    );
                  }).toList(),
                )*/